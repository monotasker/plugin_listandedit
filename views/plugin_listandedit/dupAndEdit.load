<span class='warning'>New copy: not yet saved</span>
{{vals = len(response._vars.values())
for v in range(vals): 
    =response._vars.values()[v]
    # TODO: have multiple select widgets populated properly
pass}}
